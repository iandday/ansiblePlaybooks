---
#
# roles/common/reboot.yml
#

- name: reboot system
  shell: sleep 2 && /sbin/shutdown -r now "system restart after Ansible play"
  async: 1
  poll: 0
  become: true
  
- name: wait for reboot
  wait_for_connection:
    delay: 1
    timeout: 300
  delegate_to: localhost
