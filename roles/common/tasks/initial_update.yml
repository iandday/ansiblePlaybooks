---
# roles/common/tasks/initial_update.yml

- name: install common packages
  raw: "sudo apt-get update -y && sudo apt-get install -y {{ common_packages }}"

- name: initial package upgrade
  apt:
    upgrade: yes
    update_cache: yes
    cache_valid_time: 3600
  become_user: root
  become: true